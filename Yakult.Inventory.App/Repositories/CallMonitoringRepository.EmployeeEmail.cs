using System;

namespace Yakult.Inventory.App.Repositories
{
    // Employee-email repository methods are implemented in CallMonitoringRepository.cs
    // so every project variant that compiles the core repository receives the interface members.
    public sealed partial class CallMonitoringRepository
    {
    }
}
