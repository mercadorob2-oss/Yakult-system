<Window x:Class="Yakult.Inventory.App.Pages.Set.RecordSetInvoiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Record Invoice"
        Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="FL" TargetType="TextBlock">
            <Setter Property="FontSize"          Value="11.5"/>
            <Setter Property="FontWeight"        Value="SemiBold"/>
            <Setter Property="Foreground"        Value="#4A5A6A"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin"            Value="0,0,10,0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height"                   Value="28"/>
            <Setter Property="FontSize"                 Value="12"/>
            <Setter Property="Padding"                   Value="6,0"/>
            <Setter Property="VerticalContentAlignment"  Value="Center"/>
            <Setter Property="Margin"                    Value="0,0,0,7"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height"     Value="28"/>
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="Margin"    Value="0,0,0,7"/>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Height"     Value="32"/>
            <Setter Property="Padding"    Value="18,0"/>
            <Setter Property="Background" Value="#3A8EF6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"     Value="Hand"/>
        </Style>
        <Style x:Key="SecBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="#E8ECF0"/>
            <Setter Property="Foreground" Value="#3A4A5A"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="18">
        <TextBlock Text="Record Invoice for this Set" FontSize="14" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock x:Name="TxtSetCodeLabel" FontSize="11" Foreground="#8A9AAA" Margin="0,0,0,8"/>
        <Border Background="#EEF4FF" BorderBrush="#BDD0F8" BorderThickness="1" CornerRadius="4"
                Padding="8,6" Margin="0,0,0,12">
            <TextBlock FontSize="10.5" Foreground="#2A5BC8" TextWrapping="Wrap"
                       Text="Enter the invoice number and amounts here. The invoice is addressed to a site — its Company / Department / Branch (taken from the requester), Sub-Type groups and Parent Tags are set on the invoice screen that opens next."/>
        </Border>

        <Grid Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Document No. *" Style="{StaticResource FL}"/>
            <TextBox x:Name="TxtDocumentNumber" Grid.Column="1"/>
        </Grid>

        <Grid Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Reference No." Style="{StaticResource FL}"/>
            <TextBox x:Name="TxtReferenceNumber" Grid.Column="1"/>
        </Grid>

        <Grid Margin="0,4,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Subtotal" Style="{StaticResource FL}"/>
            <TextBox x:Name="TxtSubtotal" Grid.Column="1" TextChanged="Amounts_TextChanged"/>
            <TextBlock Grid.Column="3" Text="VAT (%)" Style="{StaticResource FL}"/>
            <TextBox x:Name="TxtVatAmount" Grid.Column="4" Text="0" TextChanged="Amounts_TextChanged"/>
        </Grid>

        <Grid Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="WHT (%)" Style="{StaticResource FL}"/>
            <TextBox x:Name="TxtWhtAmount" Grid.Column="1" Text="0" TextChanged="Amounts_TextChanged"/>
            <TextBlock Grid.Column="3" Text="Discount (%)" Style="{StaticResource FL}"/>
            <TextBox x:Name="TxtDiscountAmount" Grid.Column="4" Text="0" TextChanged="Amounts_TextChanged"/>
        </Grid>

        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Total Amount Due" Style="{StaticResource FL}"/>
            <TextBox x:Name="TxtTotalAmountDue" Grid.Column="1" IsReadOnly="True" Background="#F5F7FA"/>
        </Grid>

        <Border BorderBrush="#DDE3EC" BorderThickness="0,1,0,0" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecBtn}" Margin="0,0,8,0" Click="BtnCancel_Click"/>
            <Button Content="Save" Style="{StaticResource PrimaryBtn}" Click="BtnSave_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
