<Window x:Class="Yakult.Inventory.App.Pages.Set.DeployDateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Deploy Set"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="FL" TargetType="TextBlock">
            <Setter Property="FontSize"          Value="11.5"/>
            <Setter Property="FontWeight"        Value="SemiBold"/>
            <Setter Property="Foreground"        Value="#4A5A6A"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin"            Value="0,0,10,0"/>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Height"     Value="32"/>
            <Setter Property="Padding"    Value="18,0"/>
            <Setter Property="Background" Value="#3A8EF6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"     Value="Hand"/>
        </Style>
        <Style x:Key="SecBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="#E8ECF0"/>
            <Setter Property="Foreground" Value="#3A4A5A"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="18">
        <TextBlock Text="Deploy this Set" FontSize="14" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock x:Name="TxtSetCodeLabel" FontSize="11" Foreground="#8A9AAA" Margin="0,0,0,8"/>
        <Border Background="#EEF4FF" BorderBrush="#BDD0F8" BorderThickness="1" CornerRadius="4"
                Padding="8,6" Margin="0,0,0,12">
            <TextBlock FontSize="10.5" Foreground="#2A5BC8" TextWrapping="Wrap"
                       Text="Pick the deployment date. Today deploys normally; a past date backdates the dispatch. Future dates are not allowed."/>
        </Border>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Deploy Date *" Style="{StaticResource FL}"/>
            <DatePicker x:Name="DpDeployDate" Grid.Column="1"/>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecBtn}" Margin="0,0,8,0" Click="BtnCancel_Click"/>
            <Button Content="Deploy" Style="{StaticResource PrimaryBtn}" Click="BtnDeploy_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
