<Window x:Class="Yakult.Inventory.App.Pages.Item.RepairHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Repair History"
        Width="1160" Height="720"
        MinWidth="980" MinHeight="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Segoe UI"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        ResizeMode="CanResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Yakult.Inventory.App;component/Wpf/Shared/Styles/ListPageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- local tweaks -->
            <Style x:Key="DetailField" TargetType="TextBox">
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="Background" Value="#FAFBFC"/>
                <Setter Property="BorderBrush" Value="#DDE3EC"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="6,2"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Height" Value="26"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
            <Style x:Key="DetailLabel" TargetType="TextBlock">
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#6B7A8D"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="10" Background="White" ClipToBounds="True">
        <Border.Effect><DropShadowEffect BlurRadius="30" ShadowDepth="5" Opacity="0.18" Color="#000000"/></Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="62"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#3A8EF6" CornerRadius="10,10,0,0" MouseLeftButtonDown="OnHeaderDrag" Cursor="SizeAll">
                <DockPanel Margin="18,0,10,0" VerticalAlignment="Center">
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center" Cursor="Arrow">
                        <TextBlock Text="─" FontSize="14" FontWeight="Bold" Foreground="#BDD9FF" Cursor="Hand" Padding="8,0" MouseLeftButtonDown="OnMinimizeClick"/>
                        <TextBlock Text="□" FontSize="13" FontWeight="Bold" Foreground="#BDD9FF" Cursor="Hand" Padding="8,0" MouseLeftButtonDown="OnMaximizeClick"/>
                        <TextBlock Text="✕" FontSize="14" FontWeight="Bold" Foreground="#BDD9FF" Cursor="Hand" Padding="8,0" MouseLeftButtonDown="OnCloseClick"/>
                    </StackPanel>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Repair History" FontSize="14" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="TxtSubtitle" Text="Serial: —" FontSize="11" Foreground="#D6E8FF" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Stats -->
            <Border Grid.Row="1" Background="#F7F9FC" BorderBrush="#DDE3EC" BorderThickness="0,0,0,1" Padding="14,10">
                <WrapPanel>
                    <Border Style="{StaticResource StatCard}" Padding="12,8" MinWidth="130">
                        <StackPanel><TextBlock Text="TOTAL ENTRIES" Style="{StaticResource FieldLabel}"/><TextBlock x:Name="LblTotal" Text="0" Style="{StaticResource StatCardValue}" Foreground="#1A2333"/></StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCard}" Padding="12,8" MinWidth="110">
                        <StackPanel><TextBlock Text="REPAIRED" Style="{StaticResource FieldLabel}"/><TextBlock x:Name="LblRepaired" Text="0" Style="{StaticResource StatCardValue}" Foreground="{StaticResource SuccessBrush}"/></StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCard}" Padding="12,8" MinWidth="120">
                        <StackPanel><TextBlock Text="UNREPAIRED" Style="{StaticResource FieldLabel}"/><TextBlock x:Name="LblUnrepaired" Text="0" Style="{StaticResource StatCardValue}" Foreground="{StaticResource DangerBrush}"/></StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCard}" Padding="12,8" MinWidth="110">
                        <StackPanel><TextBlock Text="SPARE" Style="{StaticResource FieldLabel}"/><TextBlock x:Name="LblSpare" Text="0" Style="{StaticResource StatCardValue}" Foreground="#3498DB"/></StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCard}" Padding="12,8" MinWidth="160">
                        <StackPanel><TextBlock Text="LAST UPDATE" Style="{StaticResource FieldLabel}"/><TextBlock x:Name="LblLast" Text="—" Style="{StaticResource StatCardValue}" FontSize="14" Foreground="#1A2333"/></StackPanel>
                    </Border>
                </WrapPanel>
            </Border>

            <!-- Filters -->
            <Border Grid.Row="2" Background="White" BorderBrush="#DDE3EC" BorderThickness="0,0,0,1" Padding="14,10">
                <DockPanel LastChildFill="False">
                    <Button DockPanel.Dock="Right" Content="Clear" Style="{StaticResource SmallBtn}" Margin="0" Click="OnClearClick"/>
                    <ComboBox x:Name="CmbAction" DockPanel.Dock="Right" Style="{StaticResource FilterCombo}" Width="150" Margin="0,0,8,0" SelectionChanged="OnFilterChanged">
                        <ComboBoxItem Content="All Actions" IsSelected="True"/>
                        <ComboBoxItem Content="Repaired"/>
                        <ComboBoxItem Content="Unrepaired"/>
                        <ComboBoxItem Content="Spare"/>
                    </ComboBox>
                    <TextBox x:Name="TxtSearch" Style="{StaticResource SearchBox}" Width="320" Margin="0,0,8,0" TextChanged="OnFilterChanged" />
                    <TextBlock Text="Search (action, remark, set/ticket, user)…" FontSize="10" Foreground="#8A94A6" VerticalAlignment="Center" Margin="6,0,0,0" IsHitTestVisible="False" x:Name="LblSearchHint"/>
                </DockPanel>
            </Border>

            <!-- Content split -->
            <Grid Grid.Row="3" Background="#F0F2F6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="360"/>
                </Grid.ColumnDefinitions>

                <!-- Grid -->
                <Border Grid.Column="0" Background="White" Margin="10,10,0,10" CornerRadius="8" BorderBrush="#DDE3EC" BorderThickness="1">
                    <Grid>
                        <DataGrid x:Name="HistoryGrid" Style="{StaticResource ListDataGridChrome}" Margin="0" IsReadOnly="True" SelectionMode="Single" AutoGenerateColumns="False" SelectionChanged="OnGridSelectionChanged" MouseDoubleClick="OnGridDoubleClick">
                            <DataGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#B8CEED"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#1A2333"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#CDD8EE"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#1A2333"/>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="135" MinWidth="130"/>
                                <DataGridTextColumn Header="Condition" Binding="{Binding ConditionName}" Width="100" MinWidth="90"/>
                                <DataGridTextColumn Header="Action" Binding="{Binding RepairAction}" Width="100" MinWidth="90"/>
                                <DataGridTextColumn Header="From" Binding="{Binding PreviousStatus}" Width="100" MinWidth="90"/>
                                <DataGridTextColumn Header="To" Binding="{Binding NewStatus}" Width="100" MinWidth="90"/>
                                <DataGridTextColumn Header="Ref" Binding="{Binding ReferenceCode}" Width="90" MinWidth="80"/>
                                <DataGridTextColumn Header="Ticket" Binding="{Binding ItCallTicketCode}" Width="100" MinWidth="90"/>
                                <DataGridTextColumn Header="Processed By" Binding="{Binding ProcessedByName}" Width="130" MinWidth="120"/>
                                <DataGridTextColumn Header="Remark" Binding="{Binding Remark}" Width="240" MinWidth="180">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource WrapCellText}">
                                            <Setter Property="ToolTip" Value="{Binding Remark}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock x:Name="TxtEmpty" Text="No repair history found for this serial." Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" FontStyle="Italic" Foreground="#8A94A6" FontSize="12"/>
                    </Grid>
                </Border>

                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ResizeBehavior="PreviousAndNext"/>

                <!-- Details -->
                <Border Grid.Column="2" Background="White" Margin="0,10,10,10" CornerRadius="8" BorderBrush="#DDE3EC" BorderThickness="1" Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="ENTRY DETAILS" FontSize="10" FontWeight="Bold" Foreground="#3A8EF6" Margin="0,0,0,10"/>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Created" Style="{StaticResource DetailLabel}"/><TextBox x:Name="TxtCreated" Grid.Column="1" Style="{StaticResource DetailField}"/></Grid>
                                <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Action" Style="{StaticResource DetailLabel}"/><TextBox x:Name="TxtAction" Grid.Column="1" Style="{StaticResource DetailField}"/></Grid>
                                <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Condition" Style="{StaticResource DetailLabel}"/><TextBox x:Name="TxtCondition" Grid.Column="1" Style="{StaticResource DetailField}"/></Grid>
                                <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Processed" Style="{StaticResource DetailLabel}"/><TextBox x:Name="TxtProcessedBy" Grid.Column="1" Style="{StaticResource DetailField}"/></Grid>
                                <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ref" Style="{StaticResource DetailLabel}"/><TextBox x:Name="TxtRef" Grid.Column="1" Style="{StaticResource DetailField}"/></Grid>
                                <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="From" Style="{StaticResource DetailLabel}"/><TextBox x:Name="TxtFrom" Grid.Column="1" Style="{StaticResource DetailField}"/></Grid>
                                <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="To" Style="{StaticResource DetailLabel}"/><TextBox x:Name="TxtTo" Grid.Column="1" Style="{StaticResource DetailField}"/></Grid>
                                <Grid Margin="0,0,0,10"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ids" Style="{StaticResource DetailLabel}"/><TextBox x:Name="TxtIds" Grid.Column="1" Style="{StaticResource DetailField}"/></Grid>

                                <TextBlock Text="Remark" FontSize="10" FontWeight="SemiBold" Foreground="#6B7A8D" Margin="0,0,0,4"/>
                                <TextBox x:Name="TxtRemark" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" MinHeight="120" Background="#FAFBFC" BorderBrush="#DDE3EC" BorderThickness="1" Padding="8,6" FontSize="11"/>
                                <TextBlock FontSize="10" Foreground="#8A94A6" Margin="0,6,0,0" TextWrapping="Wrap" Text="Double-click a Ticket cell to open the ITCALL ticket. Remark may contain [ITCALL:CODE#ID] tag."/>
                            </StackPanel>
                        </ScrollViewer>

                        <Grid Grid.Row="2" Margin="0,12,0,0">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="8"/><ColumnDefinition Width="*"/><ColumnDefinition Width="8"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Copy Remark" Style="{StaticResource SmallBtn}" Margin="0" Click="OnCopyRemarkClick"/>
                            <Button Grid.Column="2" Content="Copy Details" Style="{StaticResource SmallBtn}" Margin="0" Click="OnCopyAllClick"/>
                            <Button Grid.Column="4" Content="Close" Style="{StaticResource PrimaryBtn}" Margin="0" Click="OnCloseClickBtn"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
