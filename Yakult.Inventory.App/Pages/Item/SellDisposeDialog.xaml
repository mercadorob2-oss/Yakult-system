<Window x:Class="Yakult.Inventory.App.Pages.Item.SellDisposeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lifecycle Action"
        Width="640" Height="620"
        MinWidth="580" MinHeight="520"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Segoe UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style x:Key="FormField" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#1A2333"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#C8D2DC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True"><Setter TargetName="Bd" Property="BorderBrush" Value="#3A8EF6"/><Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Bd" Property="Background" Value="#F5F7FA"/><Setter Property="Foreground" Value="#A0ADB9"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="MultiField" TargetType="TextBox" BasedOn="{StaticResource FormField}">
            <Setter Property="Height" Value="Auto"/><Setter Property="MinHeight" Value="72"/><Setter Property="AcceptsReturn" Value="True"/><Setter Property="TextWrapping" Value="Wrap"/><Setter Property="VerticalScrollBarVisibility" Value="Auto"/><Setter Property="VerticalContentAlignment" Value="Top"/><Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style x:Key="FL" TargetType="TextBlock"><Setter Property="FontSize" Value="9.5"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Foreground" Value="#8A94A6"/><Setter Property="Margin" Value="0,0,0,3"/></Style>
        <Style x:Key="ST" TargetType="TextBlock"><Setter Property="FontSize" Value="9.5"/><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="#3A8EF6"/><Setter Property="Margin" Value="0,0,0,10"/></Style>
        <Style x:Key="Card" TargetType="Border"><Setter Property="Background" Value="White"/><Setter Property="BorderBrush" Value="#DDE3EC"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="8"/><Setter Property="Padding" Value="16,14"/><Setter Property="Margin" Value="0,0,0,10"/><Setter Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.06" Direction="270" Color="#0A1A2A"/></Setter.Value></Setter></Style>
        <Style x:Key="PrimaryBtn" TargetType="Button"><Setter Property="Height" Value="38"/><Setter Property="Padding" Value="22,0"/><Setter Property="FontSize" Value="12"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Cursor" Value="Hand"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Background" Value="#3A8EF6"/><Setter Property="Foreground" Value="White"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.88"/></Trigger><Trigger Property="IsEnabled" Value="False"><Setter TargetName="Bd" Property="Opacity" Value="0.42"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
        <Style x:Key="SecBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}"><Setter Property="Background" Value="#E8ECF0"/><Setter Property="Foreground" Value="#3A4A5A"/></Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="White" ClipToBounds="True">
        <Border.Effect><DropShadowEffect BlurRadius="30" ShadowDepth="5" Opacity="0.18" Color="#000000"/></Border.Effect>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="62"/><RowDefinition Height="*"/><RowDefinition Height="66"/></Grid.RowDefinitions>

            <Border x:Name="HeaderBar" Grid.Row="0" CornerRadius="10,10,0,0" MouseLeftButtonDown="OnHeaderDrag" Cursor="SizeAll">
                <DockPanel Margin="18,0,10,0" VerticalAlignment="Center">
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center" Cursor="Arrow">
                        <TextBlock Text="─" FontSize="14" FontWeight="Bold" Foreground="#BDD9FF" Cursor="Hand" Padding="8,0" MouseLeftButtonDown="OnMinimizeClick"/>
                        <TextBlock Text="✕" FontSize="14" FontWeight="Bold" Foreground="#BDD9FF" Cursor="Hand" Padding="8,0" MouseLeftButtonDown="OnCloseClick"/>
                    </StackPanel>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="TxtTitle" Text="Sell Item" FontSize="14" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="TxtSubtitle" Text="This will archive the item" FontSize="10" Foreground="#E8F0FF" Margin="0,2,0,0"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <ScrollViewer Grid.Row="1" Background="#F0F2F6" VerticalScrollBarVisibility="Auto" Padding="16,14,16,8">
                <StackPanel>
                    <Border x:Name="BulkBanner" Visibility="Collapsed" Background="#FFF3E6" BorderBrush="#F5C842" BorderThickness="1" CornerRadius="6" Padding="10,8" Margin="0,0,0,10">
                        <TextBlock x:Name="TxtBulkBanner" FontSize="11" Foreground="#7A4A00" TextWrapping="Wrap"/>
                    </Border>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <DockPanel Margin="0,0,0,6" LastChildFill="False">
                                <TextBlock x:Name="TxtSelectedHeader" Text="ITEMS TO ARCHIVE" Style="{StaticResource ST}" Margin="0"/>
                                <Border DockPanel.Dock="Right" Background="#E74C3C" CornerRadius="10" Padding="8,2" x:Name="CountBadge">
                                    <TextBlock x:Name="TxtSelectedCount" Text="1 item" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                </Border>
                            </DockPanel>
                            <DataGrid x:Name="SelectedItemsGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                                      HeadersVisibility="Column" GridLinesVisibility="All" AlternationCount="2" RowHeaderWidth="0"
                                      MaxHeight="210" MinHeight="52" FontSize="11" BorderBrush="#DDE3EC" BorderThickness="1"
                                      HorizontalGridLinesBrush="#EEF1F4" VerticalGridLinesBrush="#E4E8EF" Background="White"
                                      CanUserSortColumns="False" CanUserReorderColumns="False" CanUserResizeRows="False">
                                <DataGrid.ColumnHeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="#F0F2F6"/><Setter Property="Foreground" Value="#5B6A7D"/>
                                        <Setter Property="FontSize" Value="10"/><Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Padding" Value="8,6"/><Setter Property="BorderBrush" Value="#DDE3EC"/><Setter Property="BorderThickness" Value="0,0,1,1"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                    </Style>
                                </DataGrid.ColumnHeaderStyle>
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Setter Property="Background" Value="White"/><Setter Property="MinHeight" Value="28"/>
                                        <Style.Triggers>
                                            <Trigger Property="AlternationIndex" Value="1"><Setter Property="Background" Value="#FAFBFC"/></Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="1.8*" MinWidth="140"/>
                                    <DataGridTextColumn Header="Serial" Binding="{Binding SerialNumber}" Width="1*" MinWidth="100"/>
                                    <DataGridTextColumn Header="Model" Binding="{Binding ModelNumber}" Width="1*" MinWidth="100"/>
                                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="1*" MinWidth="90"/>
                                    <DataGridTextColumn Header="Condition" Binding="{Binding ConditionName}" Width="0.9*" MinWidth="85"/>
                                    <DataGridTextColumn Header="Stock" Binding="{Binding StockOnHand}" Width="0.6*" MinWidth="55"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <TextBlock x:Name="TxtMoreHint" Visibility="Collapsed" FontSize="10" Foreground="#8A94A6" Margin="0,6,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="QUANTITY" Style="{StaticResource ST}"/>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="140"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                    <TextBlock Text="QTY TO ARCHIVE *" Style="{StaticResource FL}"/>
                                    <TextBox x:Name="TxtQty" Style="{StaticResource FormField}" Text="1" PreviewTextInput="OnQtyPreviewTextInput" TextChanged="OnQtyChanged"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="0,14,0,0" FontSize="11" Foreground="#8A94A6" TextWrapping="Wrap" Text="Applied per selected item. Max is clamped to stock on hand."/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="RECIPIENT &amp; AMOUNT" Style="{StaticResource ST}"/>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="RECIPIENT (OPTIONAL)" Style="{StaticResource FL}"/>
                                <TextBox x:Name="TxtRecipient" Style="{StaticResource FormField}" />
                            </StackPanel>
                            <StackPanel x:Name="AmountPanel">
                                <TextBlock Text="SALE AMOUNT (OPTIONAL)" Style="{StaticResource FL}"/>
                                <TextBox x:Name="TxtAmount" Style="{StaticResource FormField}" PreviewTextInput="OnAmountPreviewTextInput" TextChanged="OnAmountChanged"/>
                                <TextBlock Text="Only relevant when selling — leave 0 for dispose." FontSize="10" Foreground="#8A94A6" Margin="0,4,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="REMARKS (OPTIONAL)" Style="{StaticResource ST}" Foreground="#8A94A6"/>
                            <TextBox x:Name="TxtRemarks" Style="{StaticResource MultiField}" MinHeight="70"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#FDECEA" BorderBrush="#F1B7B0" BorderThickness="1" CornerRadius="6" Padding="10,8">
                        <TextBlock x:Name="TxtWarning" FontSize="10.5" Foreground="#7A1F1A" TextWrapping="Wrap" Text="This will mark the item(s) as Sold/Disposed and archive them. You can restore from Archive later if needed."/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Background="White" BorderBrush="#DDE3EC" BorderThickness="0,1,0,0" CornerRadius="0,0,10,10">
                <DockPanel Margin="16,0" VerticalAlignment="Center" LastChildFill="False">
                    <Button x:Name="BtnOk" DockPanel.Dock="Right" Style="{StaticResource PrimaryBtn}" Click="OnOkClick" IsDefault="True">
                        <TextBlock x:Name="TxtOkLabel" Text="Confirm"/>
                    </Button>
                    <Button DockPanel.Dock="Right" Content="Cancel" Style="{StaticResource SecBtn}" Margin="0,0,8,0" Click="OnCancelClick" IsCancel="True"/>
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</Window>
