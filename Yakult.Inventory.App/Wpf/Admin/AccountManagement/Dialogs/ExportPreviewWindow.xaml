<Window x:Class="Yakult.Inventory.App.WPF.Admin.AccountManagement.Dialogs.ExportPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Preview"
        Width="1200" Height="720"
        MinWidth="800" MinHeight="480"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">

    <Window.Resources>
        <Style x:Key="FooterBtn" TargetType="Button">
            <Setter Property="Height"          Value="32"/>
            <Setter Property="Padding"         Value="16,0"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.87"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Height"                   Value="28"/>
            <Setter Property="FontSize"                 Value="11"/>
            <Setter Property="Padding"                  Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush"              Value="#C8D0DC"/>
            <Setter Property="BorderThickness"          Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#4E9AFC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"                 Value="#4E9AFC"/>
            <Setter Property="Foreground"                 Value="White"/>
            <Setter Property="FontWeight"                 Value="SemiBold"/>
            <Setter Property="FontSize"                   Value="11"/>
            <Setter Property="Padding"                    Value="8,7"/>
            <Setter Property="BorderBrush"                Value="#3A8EF6"/>
            <Setter Property="BorderThickness"            Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#FAFBFC"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsArchived}" Value="True">
                    <Setter Property="Background" Value="#E2E5EA"/>
                    <Setter Property="Foreground" Value="#7A8A9A"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#4E9AFC" Padding="20,14">
            <StackPanel>
                <TextBlock Text="Export Preview" Foreground="White" FontSize="15" FontWeight="Bold"/>
                <TextBlock x:Name="TxtSubtitle" Foreground="#EAF2FF" FontSize="11" Margin="0,3,0,0"/>
            </StackPanel>
        </Border>

        <!-- Search -->
        <Border Grid.Row="1" Background="#F8FAFB" BorderBrush="#DDE3EC" BorderThickness="0,0,0,1" Padding="16,10">
            <Grid>
                <TextBox x:Name="TxtSearch" Style="{StaticResource SearchBox}" Width="320" HorizontalAlignment="Left"
                         TextChanged="TxtSearch_TextChanged"/>
                <TextBlock x:Name="TxtRowCount" FontSize="11" Foreground="#5A6A7A" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Full-column preview grid, mirrors the exported workbook exactly -->
        <DataGrid x:Name="GridPreview"
                  Grid.Row="2"
                  Margin="16,12,16,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeight="28"
                  FontSize="11"
                  BorderThickness="1"
                  BorderBrush="#DDE3EC"
                  AlternationCount="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Emp #"          Binding="{Binding EmployeeNumber}" Width="70"/>
                <DataGridTextColumn Header="Title"          Binding="{Binding TitleCode}"       Width="55"/>
                <DataGridTextColumn Header="Name"           Binding="{Binding Name}"            Width="1.3*"/>
                <DataGridTextColumn Header="Position"       Binding="{Binding Position}"        Width="1*"/>
                <DataGridTextColumn Header="Company"        Binding="{Binding CompanyName}"     Width="0.6*"/>
                <DataGridTextColumn Header="Department"     Binding="{Binding DepartmentName}"  Width="0.9*"/>
                <DataGridTextColumn Header="Branch"         Binding="{Binding BranchName}"      Width="0.9*"/>
                <DataGridTextColumn Header="Status"         Binding="{Binding ActiveDisplay}"   Width="70"/>
                <DataGridTextColumn Header="Dept Email"     Binding="{Binding DepartmentEmail}" Width="1.1*"/>
                <DataGridTextColumn Header="Branch Email"   Binding="{Binding BranchEmail}"     Width="1.1*"/>
                <DataGridTextColumn Header="Personal Email" Binding="{Binding PrimaryEmail}"    Width="1.1*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#F8FAFB" BorderBrush="#DDE3EC" BorderThickness="0,1,0,0" Padding="20,12" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Back" Style="{StaticResource FooterBtn}"
                        Background="White" Foreground="#5A6A7A" BorderBrush="#CDD5DE"
                        Margin="0,0,8,0" Click="BtnBack_Click"/>
                <Button Content="Continue to Save" Style="{StaticResource FooterBtn}"
                        Background="#4E9AFC" Foreground="White" BorderThickness="0"
                        Click="BtnContinue_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
