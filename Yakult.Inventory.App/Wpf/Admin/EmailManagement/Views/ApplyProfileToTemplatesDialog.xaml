<Window x:Class="Yakult.Inventory.App.WPF.Admin.EmailManagement.Views.ApplyProfileToTemplatesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apply Profile to Selected Templates"
        Width="560" Height="580" MinWidth="460" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="White"
        FontFamily="Segoe UI"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <ResourceDictionary Source="/Yakult.Inventory.App;component/Wpf/Admin/EmailManagement/Styles/EmailConfigStyles.xaml"/>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Profile to apply:" FontWeight="Bold" Margin="0,0,0,6"/>
        <ComboBox Grid.Row="1" x:Name="CboProfile" Width="280" HorizontalAlignment="Left" Height="26"
                  DisplayMemberPath="ProfileName" Margin="0,0,0,14"/>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="Select templates to update:" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnSelectAll" Content="Select All" Width="88" Height="26"
                        Background="#EEF3FA" Foreground="#3A8EF6" Style="{StaticResource ActionButton}"
                        Click="BtnSelectAll_Click"/>
                <Button x:Name="BtnClearAll" Content="Clear All" Width="88" Height="26"
                        Background="#EEF3FA" Foreground="#5A6A7A" Style="{StaticResource ActionButton}"
                        Click="BtnClearAll_Click"/>
            </StackPanel>
        </Grid>

        <ListBox Grid.Row="3" x:Name="LstTemplates" BorderBrush="#CDD5DE" BorderThickness="1"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding TemplateKey}" IsChecked="{Binding IsSelected}" Margin="4,3"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="4" Margin="0,14,0,0">
            <TextBlock x:Name="LblSelectedCount" Text="0 selected" Foreground="#5A6A7A" FontSize="11" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" Content="Cancel" Width="100" Height="32"
                        Background="#EEF3FA" Foreground="#5A6A7A" Style="{StaticResource ActionButton}"
                        IsCancel="True" Click="BtnCancel_Click"/>
                <Button x:Name="BtnApply" Content="Apply" Width="100" Height="32"
                        Background="#4E9AFC" Style="{StaticResource ActionButton}"
                        IsDefault="True" Click="BtnApply_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
