<Window x:Class="Yakult.Inventory.App.Wpf.RepairPortal.PartIntake.Views.AddPartDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Part" Height="460" Width="440" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="#F0F2F6">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Yakult.Inventory.App;component/Wpf/Shared/Styles/ListPageStyles.xaml"/>
                <ResourceDictionary Source="/Yakult.Inventory.App;component/Wpf/RepairPortal/RepairPortalStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Add Part" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Custom Part Name (optional)" Style="{StaticResource FilterLabel}"/>
            <TextBox Text="{Binding CustomLabel, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" Padding="6"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Severity" Style="{StaticResource FilterLabel}"/>
            <ComboBox Style="{StaticResource FilterCombo}" ItemsSource="{Binding SeverityOptions}" SelectedItem="{Binding SelectedSeverity}" Margin="0,4,0,0" Width="160" HorizontalAlignment="Left"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="Problem Description" Style="{StaticResource FilterLabel}"/>
            <TextBox Text="{Binding ProblemDescription, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" Height="120" Margin="0,4,0,0" Padding="6"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="Initial status: Waiting Diagnosis" FontSize="11" Foreground="#94A3B8" Margin="0,10,0,0"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource RepairSecondaryBtn}" Command="{Binding CancelCommand}" Margin="0,0,8,0"/>
            <Button Content="Save Part" Style="{StaticResource RepairPrimaryBtn}" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
