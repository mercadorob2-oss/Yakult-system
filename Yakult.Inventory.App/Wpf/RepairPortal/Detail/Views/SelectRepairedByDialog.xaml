<Window x:Class="Yakult.Inventory.App.Wpf.RepairPortal.Detail.Views.SelectRepairedByDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Repaired By" Height="480" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="White">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Repaired By" FontSize="15" FontWeight="Bold" Foreground="#1E293B"/>
        <TextBlock Grid.Row="1" Text="Select every IT Dept. employee who worked on this repair." FontSize="11.5" Foreground="#64748B" Margin="0,2,0,10"/>

        <Border Grid.Row="2" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6">
            <ListBox x:Name="EmployeeList" BorderThickness="0" ItemsSource="{Binding Items}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}" Padding="6,4" Margin="4,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" Width="86" Height="32" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="OK" Width="86" Height="32" Background="#4E9AFC" Foreground="White" BorderThickness="0" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
