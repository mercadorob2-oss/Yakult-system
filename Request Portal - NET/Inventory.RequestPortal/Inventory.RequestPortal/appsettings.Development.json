{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Database (Development override): local YIMS_PROD via Windows Auth on "." (default instance)
  // Real credentials do NOT belong here — this file is committed to git. Put the
  // actual connection string in appsettings.Local.json (gitignored, see
  // appsettings.Local.json.example) or the ConnectionStrings__DefaultConnection
  // environment variable.
  "ConnectionStrings": {
    "DefaultConnection": "",
    "LocalConnection": "Server=.;Integrated Security=True;TrustServerCertificate=True;Encrypt=False;Connection Timeout=10;"
  }
}
