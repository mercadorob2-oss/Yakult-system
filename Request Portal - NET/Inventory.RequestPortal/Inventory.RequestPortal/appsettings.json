{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "BaseUrl": "http://192.168.100.186:8080",
  "AllowedOrigins": [
    "http://192.168.100.186:8080"
  ],
  // Database: Yakult_Inventory_System_DEV on 192.168.100.186 port 50301
  // Real credentials do NOT belong here — this file is committed to git. Put the
  // actual connection string in appsettings.Local.json (gitignored, see
  // appsettings.Local.json.example) or the ConnectionStrings__DefaultConnection
  // environment variable on the deployment machine.
  "ConnectionStrings": {
    "DefaultConnection": ""
  }
} 
